coverage:
  status:
    project:
      default:
        informational: true
        target: 80%
        threshold: 2%
    patch:
      default:
        informational: true
        target: 80%
        threshold: 5%

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
